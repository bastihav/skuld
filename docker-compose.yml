version: "3.8"
services:
  skuld:
    build: ./server
    ports:
      - "8080:8080"
    volumes:
      - G:/skuld:/trees
  proxy:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
  #web-app:
   # build: ./web-app
    #ports:
     # - "443:443"
